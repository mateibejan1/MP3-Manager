<form method="post" action="{{ path('mp3metadata_delete', {'id': mp3metadata.id}) }}" onsubmit="return confirm('Doriti sa stergeti fisierul curent?');">
    <input type="hidden" name="_method" value="DELETE" >
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mp3metadata.id) }}" >
    <button class="btn" style="background-color: black ; color:white; text-align: center ; margin-left: 600px" >Delete</button>
</form>
