{% extends 'base.html.twig' %}

{% block title %}Delete MP3Metadata{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
    <table class="table" style="background-color: aliceblue">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mp3metadata.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ mp3metadata.title }}</td>
            </tr>
            <tr>
                <th>Artist</th>
                <td>{{ mp3metadata.artist }}</td>
            </tr>
            <tr>
                <th>Album</th>
                <td>{{ mp3metadata.album }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ mp3metadata.duration }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ mp3metadata.year }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ mp3metadata.genre }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ mp3metadata.comment }}</td>
            </tr>
            <tr>
                <th>Contributor</th>
                <td>{{ mp3metadata.contributor }}</td>
            </tr>
            <tr>
                <th>Bitrate</th>
                <td>{{ mp3metadata.bitrate }}</td>
            </tr>
            <tr>
                <th>Track</th>
                <td>{{ mp3metadata.track }}</td>
            </tr>
            <tr>
                <th>PopularityMeter</th>
                <td>{{ mp3metadata.popularityMeter }}</td>
            </tr>
            <tr>
                <th>UniqueFileIdentifier</th>
                <td>{{ mp3metadata.uniqueFileIdentifier }}</td>
            </tr>
        </tbody>
    </table>
    </div>
</div>

    {#<a href="{{ path('mp3metadataindex') }}">home</a>#}

    {#<a href="{{ path('mp3metadataedit', {'id': mp3metadata.id}) }}">edit</a>#}

    {{ include('mp3_meta/_delete_form.html.twig') }}
{% endblock %}
